# ============================================================
# Dokter Penjaga — Requirements
# Emergency-Aware Medical RAG Agent | INaAI 2026
# ============================================================

# --- API Framework ---
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
python-multipart>=0.0.9        # fastapi form/file upload support
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0

# --- Vector Database ---
qdrant-client>=1.9.0,<2.0.0

# --- Embedding ---
sentence-transformers>=3.0.0,<4.0.0   # Local embedding model (offline fallback)
openai>=1.30.0,<2.0.0                  # Optional: for OpenAI embeddings

# --- Keyword Search ---
rank-bm25>=0.2.2                       # BM25 implementation

# --- PII Redaction (Microsoft Presidio) ---
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
spacy>=3.7.0,<4.0.0                    # NLP backbone for Presidio

# --- LLM Integration ---
anthropic>=0.28.0,<1.0.0               # Claude via Anthropic API

# --- Configuration & Environment ---
python-dotenv>=1.0.1,<2.0.0
pyyaml>=6.0.1,<7.0.0                   # For triage_keywords.yaml & guardrail_patterns.yaml

# --- Chunking & Document Parsing ---
tiktoken>=0.7.0                        # Token counting for chunking
pypdf>=4.2.0                           # PDF parsing
python-docx>=1.1.0                     # DOCX parsing

# --- Logging & Audit ---
structlog>=24.1.0                      # Structured JSON logging

# --- HTTP Client ---
httpx>=0.27.0                          # Async HTTP client for API calls
aiofiles>=23.2.1                       # Async file I/O

# --- Utilities ---
numpy>=1.26.0,<3.0.0
tqdm>=4.66.0                           # Progress bars for ingestion

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# ============================================================
# POST-INSTALL STEP (run manually after pip install -r requirements.txt):
#   python -m spacy download xx_ent_wiki_sm   # Multilingual model (supports Indonesian)
#   python -m spacy download en_core_web_sm   # English model
#
# NOTE: id_core_news_lg / id_core_news_md are NOT available for spaCy 3.8.
#       Use xx_ent_wiki_sm as the multilingual NER backbone for Presidio.
# ============================================================
